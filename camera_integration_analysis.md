# 植物画像美化カメラ「はなみっけ」搭載検討書 (v1.1)

本ドキュメントは、ご提示いただいた追加要望・方針に基づき、「植物美化カメラ」の搭載におけるデータ連携、画像保存、AI判定フロー、および技術的懸念事項（WebView破棄対策のデメリット、OpenGL ES画像処理）をさらに深く検証したものです。

---

## 1. 意思決定事項の整理

ご提示いただいた以下の方針に基づき、仕様書 [plant_beautifier_spec.md](file:///C:/Users/Owner/Documents/Antigravity/SPC/docs/plant_beautifier_spec.md) に反映しました。

1.  **アプローチA（Capacitorカスタムプラグインによるネイティブ起動）**の採用。
2.  **撮影後画面（プリセット調整）のUI**はネイティブ（Jetpack Compose）で構築。
3.  **元画像・補正画像ともにMediaStoreに保存**（ユーザーのクラウド同期・ギャラリー利用を最優先）。
4.  **AI（Gemini）判定に送る画像は「美化前（無補正）」**の縮小画像を使用。
5.  **iOS対応は考慮しない**（Android専用機能として最適化）。

---

## 2. 検討論点・回答

### ① Web-Native間のデータ連携・API定義の懸念点
> **ユーザーの想定**: Gemini APIキーだけをネイティブに渡し、AI判定の送信もネイティブ側に任せてしまえば、Web側とのデータ連携APIは基本的に不要（シンプルになる）ではないか？

#### 結論：Web側に「鑑定結果」を戻すための重要なAPI定義が必要です。

AI送信処理自体をネイティブ側で行う設計は、バックグラウンドでの安定性（大容量アップロード時の堅牢性）の観点から非常に優れた選択です。しかし、**「APIが全く不要」にはならず、以下の点で思慮・考慮が必要です。**

1.  **鑑定結果をWeb（はなみっけ）のゲームシステムに引き渡す必要がある**:
    *   Gemini APIが返した鑑定結果（和名、学名、レア度、博士のコメントなど）をもとに、Web側（JavaScript）で「図鑑データを保存する」「コインを増やす」「画面に判定結果を表示する」といった処理を行う必要があります。
    *   そのため、カメラが閉じた際に、ネイティブからWeb側に対して**「鑑定結果のJSONデータ」および「保存されたMediaStoreのURI」を返すAPI（コールバック）の定義が必須**になります。
2.  **ビジネスロジック（プロンプト等）の二重管理リスク**:
    *   現在のはなみっけ（`gemini.js`）には、植物以外の写真が送られた時の「おちゃめな見立て鑑定」プロンプトや、猫の植物学者「ニャルド博士」の語尾指示、レア度に応じたスコアリングロジックが含まれています。
    *   AIの送信をネイティブ側で行う場合、これらの**プロンプトや鑑定ロジック（JSON Schema含む）をKotlin側でも実装・管理する**必要があります。
    *   将来的にプロンプトや判定ロジックを調整する際、Web（JS）とネイティブ（Kotlin）の両方を修正する必要があり、特にネイティブ側の修正はGoogle Playのアプリアップデート（審査）を通さなければユーザーに反映されないため、運用の機動性が低下します。
3.  **【解決策案】動的インジェクション**:
    *   プロンプトの修正をWeb側で一元管理できるようにするため、Web側からカメラプラグインを起動する際、**「Gemini APIキー」と一緒に「使用するシステムプロンプトやJSON Schema」をネイティブ側にパラメータとして渡す**設計を推奨します。これにより、ネイティブ側は「渡されたプロンプトを使ってGeminiを叩き、結果をWebに返すだけの汎用APIクライアント」として振る舞うことができ、二重管理を防げます。

---

### ② ファイル管理：MediaStore保存における不足考慮

元画像・補正画像を共にMediaStoreに保存する方針は、ユーザーの利便性において最適です。しかし、以下の実装仕様を追加で考慮する必要があります。

1.  **ファイル名による紐付け規則**:
    *   ギャラリー内で「同じタイミングで撮った元画像と補正画像」であることを識別できるようにするため、ファイル名を共通のタイムスタンプ＋接尾辞（Suffix）で管理する必要があります。
        *   元画像: `HANAMIKKE_YYYYMMDD_HHMMSS_RAW.jpg`
        *   補正後: `HANAMIKKE_YYYYMMDD_HHMMSS.jpg`
2.  **ユーザーによる手動削除時の図鑑フォールバック**:
    *   ユーザーがGoogleフォトや標準ギャラリーから写真を削除した場合、はなみっけの図鑑（IndexedDBにはURIのみ保存されている）で画像リンクが切れます。
    *   Web側の画像表示処理において、MediaStore URIからの読み込みエラーを検知し、自動的にデフォルトの「プレースホルダー画像（ニャルド博士のイラストなど）」に差し替えるフォールバック表示ロジックを実装する必要があります。

---

### ③ WebViewプロセス破棄（Process Recreation）対策のデメリット
> **ユーザーの質問**: 提案された対策（localStorageへの一時退避＋ディスクキャッシュ経由の復帰）のデメリットは？

プロセス破棄からの復旧処理（ステートセーブ＆ロード）を実装するにあたり、以下のデメリット・トレードオフが生じます。

1.  **開発およびテストの難易度・工数が大幅に上昇する**:
    *   カメラ画面に行く直前の複雑なゲーム状態（例：温室でどの親植物A・Bを選択していたか、図鑑のどのタブを開いていたか、プレイヤーの進行状況など）をすべてJSON等にシリアライズし、アプリ再起動時に完全に復元するコードを書く必要があります。
    *   Android OSのプロセス強制終了はデバッグ環境で再現させるのが難しく、復帰時の状態不整合（バグ）の温床になりやすいです。
2.  **ユーザーの待たされ感（UXの低下）**:
    *   プロセスが破棄されて再起動する場合、カメラが閉じた後に「はなみっけのスプラッシュ画面（ロード画面）」が最初から表示され、JavaScriptのロードやIndexedDBの初期化が走り直します。
    *   直接「鑑定結果画面」に遷移するとはいえ、復帰までに数秒〜十数秒かかるため、ユーザーに「アプリがクラッシュした」「動作が重い」という印象を与える可能性があります。

---

### ④ 画像処理にOpenGL ES (GLSL Shader) を採用する懸念（技術相談）

OpenGL ESを用いたGPU画像処理パイプライン（理想案）について、開発における懸念点と現実的な代替案を整理します。

#### 懸念点：実装の複雑度とバグのリスクが極めて高い
1.  **YUVからRGBへの変換処理**:
    *   CameraX（ImageAnalysis）から得られるフレームは通常 `YUV_420_888` フォーマットです。これをOpenGLで処理するには、YUVテクスチャをRGBテクスチャに変換するカスタムシェーダーを書くか、Androidの `SurfaceTexture` （OESテクスチャ）を中継する複雑なバインド処理が必要です。
2.  **複雑な画像処理（領域マスクやCLAHE）のGLSL実装難易度**:
    *   「緑・花・空」のソフトマスク抽出や、局所コントラスト補正（CLAHE）といった近傍画素を参照する複雑なアルゴリズムをGLSLシェーダーでゼロから記述・最適化するのは非常に高度なシェーダー開発知識が必要です。
3.  **ライフサイクルとコンテキスト管理の難しさ**:
    *   AndroidのActivity/Fragmentのライフサイクルと、OpenGLのEGLコンテキストの生成・破棄タイミングの同期ミスによるメモリリークやクラッシュ（画面が真っ白・真っ黒になる現象）が多発しやすいです。

#### 推奨する代替アプローチ
*   **代替案1：OpenCV Android SDK (Native C++) の利用（最も現実的）**
    *   領域マスクの生成、色補正、CLAHE、アンシャープマスクなど、仕様書にある画像処理のほとんどはOpenCVの標準APIに実装されています。
    *   OpenCVは内部でCPU（NEON命令等）向けに最適化されており、2048px解像度の処理でも、Kotlinのコルーチン（バックグラウンドスレッド）で動かせば数百ミリ秒〜1秒程度で安定して処理可能です。OpenGLでスクラッチ開発するのに比べ、**開発工数を1/5以下に圧縮でき、バグのリスクを最小限に抑えられます。**
*   **代替案2：GPUImage for Android ライブラリの導入**
    *   どうしてもOpenGLによる超高速処理を行いたい場合、オープンソースライブラリである `jp.co.cyberagent:gpuimage` の利用を強く推奨します。EGLコンテキストの管理やテクスチャのバインド、主要なフィルタ（明るさ、トーンカーブ、シャープネス）が最初からOpenGL ESベースで実装されているため、開発難易度を大幅に下げることができます。

---

## 3. 次のステップ

1.  **AI鑑定時のプロンプト・スキーマ管理の方針合意**:
    *   ネイティブにGemini API呼び出しを任せる際、「プロンプトやJSON Schema」をWeb側からネイティブ起動時にパラメータとして渡す方針にするかを決定する。
2.  **画像処理エンジンの選定**:
    *   OpenGL ESのスクラッチ開発を避け、**「OpenCV Android SDK」**または**「GPUImage for Android」**のいずれかを採用する方針へ切り替えるかの検討。
3.  **WebViewプロセス破棄対策の実装判断**:
    *   「復旧処理の開発コスト」と「アプリ起動時のロード時間」を許容し、自動復帰ロジック（`pending_scan.json` 方式）を実装するかどうかを最終決定する。
